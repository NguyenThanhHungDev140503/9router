---
phase: 02-mcp-process-manager-json-rpc-client
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/lib/db/schema.js
  - src/lib/db/migrations/003-mcp-process-policy.js
  - src/lib/db/migrations/index.js
  - src/lib/db/repos/mcpRepo.js
  - tests/unit/mcp-skills-db.test.js
autonomous: true
requirements:
  - MCP-PROC-02
  - MCP-PROC-03
must_haves:
  truths:
    - "Only configuration that passed test and was explicitly enabled can be selected for MCP runtime."
    - "Recovered server state and fresh tool cache publish only after complete discovery succeeds."
    - "Gateway retains sanitized operation metadata 30 days or less and opt-in redacted debug capture 24 hours or less."
  artifacts:
    - path: src/lib/db/migrations/003-mcp-process-policy.js
      provides: "Safe MCP configuration, lifecycle state, audit, debug retention database migration"
      contains: "version: 3"
    - path: src/lib/db/repos/mcpRepo.js
      provides: "Validated runtime configuration, state/cache, operation and debug retention repository API"
      exports: ["recordMcpServerTestResult", "updateMcpServerState", "recordMcpOperation", "purgeExpiredMcpObservability"]
    - path: tests/unit/mcp-skills-db.test.js
      provides: "Persistent MCP policy and retention tests"
      min_lines: 230
  key_links:
    - from: src/lib/db/repos/mcpRepo.js
      to: src/lib/mcp/policy.js
      via: "validate saved runtime server configuration"
      pattern: "validateMcpServerConfig"
    - from: src/lib/mcp/processManager.js
      to: src/lib/db/repos/mcpRepo.js
      via: "ready cache/state/audit persistence"
      pattern: "saveMcpToolsCache|updateMcpServerState|recordMcpOperation"
---

<objective>
Migrate Phase 1 MCP persistence from free-form spawn fields to validated runtime configuration, state, and retained observability.

Purpose: Make unsafe legacy configuration non-runnable and give manager durable state/cache/audit contracts.
Output: Schema v3 migration, repository APIs, regression tests.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mcp-process-manager-json-rpc-client/02-CONTEXT.md
@.planning/phases/02-mcp-process-manager-json-rpc-client/02-RESEARCH.md
@.planning/phases/02-mcp-process-manager-json-rpc-client/02-PATTERNS.md
@.planning/phases/01-database-and-repositories/01-01-SUMMARY.md
@src/lib/mcp/policy.js
@src/lib/db/schema.js
@src/lib/db/repos/mcpRepo.js

<interfaces>
Repository contracts for Plan 05:
- `getEnabledMcpServers()` returns only enabled, tested, policy-valid configurations; runtime secrets are not in public row maps.
- `getMcpServerRuntimeConfigById(id)` is manager-only runtime read.
- `recordMcpServerTestResult(id, result)` persists pass/fail and resets eligibility after config change.
- `updateMcpServerState(id, state, metadata)` persists `Stopped|Starting|Ready|Degraded|Error`, healthy time, sanitized error code, next retry.
- `saveMcpToolsCache(serverId, tools)` is called only after complete discovery.
- `recordMcpOperation(metadata)` and `saveMcpDebugCapture(capture)` retain sanitized metadata and opt-in redacted debug data; `purgeExpiredMcpObservability()` enforces TTL caps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand repository tests for safe runtime configuration and retention</name>
  <files>tests/unit/mcp-skills-db.test.js</files>
  <read_first>
    - tests/unit/mcp-skills-db.test.js
    - src/lib/db/repos/mcpRepo.js
    - src/lib/db/schema.js
    - src/lib/mcp/policy.js
    - .planning/phases/02-mcp-process-manager-json-rpc-client/02-CONTEXT.md
  </read_first>
  <behavior>
    - `commandId` plus schema-valid values passes; legacy free-form `command`/`args`, unknown command ID, invalid values, and unallowlisted env keys fail.
    - Saving/changing runtime configuration clears passing test status and cannot set `enabled: true` until a successful explicit test record exists.
    - State writes accept only D-28 states; fresh tool cache saves after a successful discovery caller and stale cache remains distinguishable as admin-only metadata.
    - Sanitized operation metadata expires at 30 days maximum; opt-in redacted debug capture expires at 24 hours maximum; records never expose secret/raw payload fields.
  </behavior>
  <acceptance_criteria>
    - Existing free-form stdio fixture is replaced with host-policy command ID and validated placeholders.
    - Tests cover `Stopped`, `Starting`, `Ready`, `Degraded`, `Error`; reject unknown state.
    - `npm --prefix tests exec vitest run unit/mcp-skills-db.test.js` initially fails for missing v3 repository behavior.
  </acceptance_criteria>
  <action>Update isolated SQLite tests for D-05 through D-08, D-13, and D-27 through D-30. Inject a deterministic host policy fixture before importing database modules. Assert direct SQL-facing records store no raw operation args/results by default; debug capture is opt-in, redacted, and expires. Keep existing skills/gateway-rule tests intact.</action>
  <verify>
    <automated>npm --prefix tests exec vitest run unit/mcp-skills-db.test.js</automated>
  </verify>
  <done>Database tests define valid command-ID configuration, tested-enable gate, lifecycle/cache state, sanitized audit metadata, and hard retention behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema v3 migration and policy-aware MCP repository API</name>
  <files>src/lib/db/schema.js, src/lib/db/migrations/003-mcp-process-policy.js, src/lib/db/migrations/index.js, src/lib/db/repos/mcpRepo.js, tests/unit/mcp-skills-db.test.js</files>
  <read_first>
    - src/lib/db/schema.js
    - src/lib/db/migrations/002-mcp-skills.js
    - src/lib/db/migrations/index.js
    - src/lib/db/repos/mcpRepo.js
    - src/lib/mcp/policy.js
    - tests/unit/mcp-skills-db.test.js
    - src/lib/db/repos/requestDetailsRepo.js
  </read_first>
  <behavior>
    - Fresh database schema and migrated Phase 1 database expose same safe MCP columns/tables.
    - Legacy rows cannot spawn until migrated to valid command ID/tested config; old data is retained but disabled/error-marked rather than silently executed.
    - Cache publication and operation/debug retention use parameterized SQL and transactions.
  </behavior>
  <acceptance_criteria>
    - `SCHEMA_VERSION` becomes `3`; `MIGRATIONS` includes unique monotonic migration version `3`.
    - `mcpServers` stores `commandId`, JSON placeholder values, test status/time, and D-28 state metadata; it does not grant runtime authority to legacy `command` or `args`.
    - Migration creates indexed operation metadata and debug-capture retention tables; metadata retention never exceeds 30 days and debug retention never exceeds 24 hours.
    - `npm --prefix tests exec vitest run unit/mcp-skills-db.test.js` passes.
  </acceptance_criteria>
  <action>Update schema v3 and add migration `003-mcp-process-policy.js` using safe additive column/table operations compatible with existing SQLite migration runner. Extend `mcpRepo.js` to validate create/update through `validateMcpServerConfig`, separate redacted public row mapping from manager-only runtime config reads, reset test approval when executable/values/env/url/transport changes, and refuse explicit enable without latest passed test per D-08. Persist allowed D-28 state fields and save tools only when manager passes full discovery result. Add transaction-backed sanitized `mcpOperations` metadata and opt-in redacted `mcpDebugCaptures`, plus TTL purge hard-capped at D-30. Preserve async API, ISO timestamps, JSON helper usage, SQL parameters, cache cascade deletion, and all existing skills APIs.</action>
  <verify>
    <automated>npm --prefix tests exec vitest run unit/mcp-skills-db.test.js</automated>
  </verify>
  <done>Schema v3 and repository pass isolated SQLite tests; free-form legacy spawn configuration cannot become runnable; manager has tested-enable, state/cache, audit, and bounded-retention persistence contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard persistence → manager runtime | Saved records become candidate process/remote connection configuration. |
| manager → SQLite audit storage | Runtime events and potentially sensitive debug inputs/results cross into durable storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | `mcpRepo.js` save/enable path | mitigate | Validate command ID/values through host policy; reset test status after config change; refuse untested enable. |
| T-02-09 | Information Disclosure | MCP audit/debug tables | mitigate | Store metadata only by default; opt-in redacted debug capture; separate internal admin query surface; TTL purge. |
| T-02-10 | Tampering | migration/repository SQL | mitigate | Additive migration, parameterized SQL, transactions, state enum validation, and tested SQLite regression coverage. |
| T-02-11 | Denial of Service | retained records | mitigate | Indexed expiration columns and automatic 30-day/24-hour bounded retention cleanup. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan. |
</threat_model>

<verification>
Run `npm --prefix tests exec vitest run unit/mcp-skills-db.test.js`. Confirm migration registry includes version 3 and repository never passes legacy `command`/`args` to runtime launch contract.
</verification>

<success_criteria>
Persisted MCP settings require host policy and successful test before enablement; lifecycle/cache/audit records are durable, sanitized, and automatically bounded.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-process-manager-json-rpc-client/02-03-SUMMARY.md` when done.
</output>
