---
phase: 04-autonomous-server-side-react-loop
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - open-sse/mcp/contextInjector.js
  - open-sse/mcp/toolExecutor.js
  - open-sse/mcp/usageAccumulator.js
  - tests/unit/mcp-context-injector.test.js
  - tests/unit/mcp-tool-executor.test.js
autonomous: true
requirements:
  - MCP-REACT-03
  - MCP-REACT-05
user_setup: []
must_haves:
  truths:
    - "Assistant tool calls and tool result messages are formatted natively according to the request's `sourceFormat`."
    - "OpenAI formats use role `assistant` with `tool_calls` and role `tool` with `tool_call_id`."
    - "Claude formats use role `assistant` with `tool_use` blocks and role `user` with `tool_result` blocks."
    - "Gemini formats use role `model` with `functionCall` parts and role `user` with `functionResponse` parts."
    - "Responses API formats use `function_call` items and `function_call_output` items."
    - "`toolExecutor` calls `processManager.callServerTool()` with error trapping and soft error formatting."
    - "`usageAccumulator` sums token usages across turns into standard usage shapes."
  artifacts:
    - path: "open-sse/mcp/contextInjector.js"
      provides: "Native source-format message history appending for intermediate ReAct turns."
      exports: ["injectTurnHistory"]
    - path: "open-sse/mcp/toolExecutor.js"
      provides: "Safe execution of MCP tool calls via ProcessManager with timeout/crash protection."
      exports: ["executeMcpToolCall", "executeMcpToolCalls"]
    - path: "open-sse/mcp/usageAccumulator.js"
      provides: "Token usage accumulation across multi-turn ReAct loops."
      exports: ["UsageAccumulator"]
    - path: "tests/unit/mcp-context-injector.test.js"
      provides: "Unit tests for format-aware context feeding across OpenAI, Claude, Gemini, and Responses formats."
    - path: "tests/unit/mcp-tool-executor.test.js"
      provides: "Unit tests for MCP tool execution, error soft landing, and token accumulation."
  key_links:
    - from: "open-sse/mcp/toolExecutor.js"
      to: "src/lib/mcp/processManager.js"
      via: "`getProcessManager().callServerTool`."
      pattern: "getProcessManager|callServerTool"
    - from: "open-sse/mcp/contextInjector.js"
      to: "open-sse/translator/formats.js"
      via: "handles formatting for each native format."
      pattern: "FORMATS"
---

<objective>
TDD context injection, tool execution wrapper, and usage accumulation.

Purpose: Meet MCP-REACT-03 and MCP-REACT-05 by providing format-native message shaping for intermediate turns, fail-safe tool execution, and cumulative token tracking.
Output: `contextInjector.js`, `toolExecutor.js`, `usageAccumulator.js` with comprehensive unit tests.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autonomous-server-side-react-loop/04-CONTEXT.md
@.planning/phases/04-autonomous-server-side-react-loop/04-RESEARCH.md
@src/lib/mcp/processManager.js
@open-sse/translator/formats.js

<interfaces>
From `src/lib/mcp/processManager.js`:
`getProcessManager().callServerTool(serverId, toolName, args, meta)` returns MCP result:
`{ content: Array<{ type: "text", text: string }>, isError?: boolean }`

Native format shaping contracts:
1. OpenAI:
   - Assistant: `{ role: "assistant", content: null, tool_calls: [{ id, type: "function", function: { name, arguments: JSON.stringify(args) } }] }`
   - Tool Result: `{ role: "tool", tool_call_id: id, content: string }`
2. Claude:
   - Assistant: `{ role: "assistant", content: [{ type: "tool_use", id, name, input: args }] }`
   - Tool Result: `{ role: "user", content: [{ type: "tool_result", tool_use_id: id, content: string, is_error?: boolean }] }`
3. Gemini:
   - Assistant: `{ role: "model", parts: [{ functionCall: { name, args } }] }`
   - Tool Result: `{ role: "user", parts: [{ functionResponse: { name, response: { result: string } } }] }`
4. Responses API:
   - Assistant Item: `{ type: "function_call", call_id: id, name, arguments: JSON.stringify(args) }`
   - Tool Result Item: `{ type: "function_call_output", call_id: id, output: string }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for format-aware context injection and tool execution</name>
  <files>tests/unit/mcp-context-injector.test.js, tests/unit/mcp-tool-executor.test.js</files>
  <behavior>
    - `injectTurnHistory` immutably appends assistant tool calls and tool results for OpenAI format bodies (`body.messages`).
    - `injectTurnHistory` immutably appends assistant tool use and tool results for Claude format bodies (`body.messages`).
    - `injectTurnHistory` immutably appends model function call and function response for Gemini format bodies (`body.contents`).
    - `injectTurnHistory` immutably appends function call items and output items for Responses API format bodies (`body.input`).
    - `executeMcpToolCall` invokes `getProcessManager().callServerTool()` and formats output text.
    - `executeMcpToolCall` catches errors (server offline, timeout, execution failure) and returns a formatted error object with `isError: true` without throwing.
    - `UsageAccumulator` adds usages and computes cumulative prompt_tokens, completion_tokens, total_tokens.
  </behavior>
  <action>Create RED Vitest tests in `tests/unit/mcp-context-injector.test.js` and `tests/unit/mcp-tool-executor.test.js` asserting correct message shapes and error trapping behavior before implementation.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-context-injector.test.js tests/unit/mcp-tool-executor.test.js</automated>
  </verify>
  <done>Tests are written and fail due to missing modules or exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contextInjector, toolExecutor, and usageAccumulator</name>
  <files>open-sse/mcp/contextInjector.js, open-sse/mcp/toolExecutor.js, open-sse/mcp/usageAccumulator.js</files>
  <behavior>
    - All Task 1 RED unit tests pass GREEN.
    - `contextInjector.js` performs copy-on-write injection across all supported formats.
    - `toolExecutor.js` coordinates execution with processManager safely.
    - `usageAccumulator.js` provides clean aggregation methods.
  </behavior>
  <action>Implement `injectTurnHistory` in `contextInjector.js`. Implement `executeMcpToolCall` and `executeMcpToolCalls` in `toolExecutor.js` with error soft-landing. Implement `UsageAccumulator` in `usageAccumulator.js`.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-context-injector.test.js tests/unit/mcp-tool-executor.test.js</automated>
  </verify>
  <done>Context injection, tool execution, and usage accumulation unit tests pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Process / Server output → Gateway context | Output from MCP tools may contain large strings or unexpected types. |
| Model request history → Upstream LLM | Injected messages must not corrupt original conversation history structure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `injectTurnHistory` | mitigate | Use copy-on-write immutable data manipulation; do not mutate existing message objects or arrays. |
| T-04-05 | Denial of Service | `toolExecutor` | mitigate | Wrap tool execution in try/catch; format errors as standard string results rather than crashing the request. |
| T-04-06 | Information Disclosure | `executeMcpToolCall` | mitigate | Sanitize process manager errors before feeding them into prompt context. |
</threat_model>

<verification>
Run `npm --prefix tests test -- tests/unit/mcp-context-injector.test.js tests/unit/mcp-tool-executor.test.js`.
</verification>

<success_criteria>
MCP-REACT-03 and MCP-REACT-05 requirements for format-native context feeding, tool execution, and usage accumulation are implemented and tested.
</success_criteria>

<output>
Create `.planning/phases/04-autonomous-server-side-react-loop/04-02-SUMMARY.md` upon completion.
</output>
