---
phase: 04-autonomous-server-side-react-loop
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - open-sse/config/mcpConstants.js
  - open-sse/mcp/toolPartition.js
  - tests/unit/mcp-tool-partition.test.js
autonomous: true
requirements:
  - MCP-REACT-01
  - MCP-REACT-02
user_setup: []
must_haves:
  truths:
    - "Tool calls prefixed with `mcp__` are identified and extracted as Gateway MCP calls."
    - "Tool calls without `mcp__` prefix are identified as Client Native calls and never intercepted."
    - "Tool names with `mcp__{server}__{tool}` are parsed reliably into server ID and tool name."
    - "Tool extraction supports parsed non-streaming JSON responses across OpenAI, Claude, Gemini, and Responses formats."
  artifacts:
    - path: "open-sse/config/mcpConstants.js"
      provides: "Constants for ReAct loop, iteration limits, and MCP prefixes."
      exports: ["MAX_REACT_ITERATIONS", "MCP_TOOL_PREFIX"]
    - path: "open-sse/mcp/toolPartition.js"
      provides: "Pure extraction, parsing, and partitioning of tool calls across model response shapes."
      exports: ["parseNamespacedToolName", "extractToolCallsFromResponse", "partitionToolCalls", "isMcpToolName"]
    - path: "tests/unit/mcp-tool-partition.test.js"
      provides: "Unit tests covering tool call extraction, namespacing parsing, and MCP vs client partitioning."
  key_links:
    - from: "open-sse/mcp/toolPartition.js"
      to: "open-sse/config/mcpConstants.js"
      via: "imports `MCP_TOOL_PREFIX`."
      pattern: "import.*MCP_TOOL_PREFIX.*mcpConstants"
    - from: "open-sse/mcp/toolPartition.js"
      to: "open-sse/translator/formats.js"
      via: "handles response shapes corresponding to format definitions."
      pattern: "FORMATS"
---

<objective>
TDD tool call extraction, namespaced name parsing, and partitioning of MCP vs client-native tool calls.

Purpose: Meet MCP-REACT-01 and MCP-REACT-02 by providing robust, format-aware extraction of tool calls from upstream LLM responses and partitioning them cleanly.
Output: `open-sse/mcp/toolPartition.js` with comprehensive unit test coverage.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autonomous-server-side-react-loop/04-CONTEXT.md
@.planning/phases/04-autonomous-server-side-react-loop/04-RESEARCH.md
@open-sse/AGENTS.md
@open-sse/config/mcpConstants.js
@open-sse/mcp/injector.js
@open-sse/translator/formats.js

<interfaces>
From `open-sse/config/mcpConstants.js`:
Export `MAX_REACT_ITERATIONS = 10` and `MCP_TOOL_PREFIX = "mcp__"`.

From `open-sse/mcp/injector.js`:
`namespaceToolName(serverId, toolName)` produces `mcp__{server}__{tool}`.

Tool call representation normalized:
`{ id: string, name: string, args: object|string, raw: any }`

Partitioning result:
`{ mcpCalls: Array<{ id, name, serverId, toolName, args }>, clientCalls: Array<{ id, name, args }> }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for tool extraction, parsing, and partitioning</name>
  <files>tests/unit/mcp-tool-partition.test.js</files>
  <behavior>
    - `parseNamespacedToolName` correctly parses `mcp__filesystem__read_file` into `{ serverId: "filesystem", toolName: "read_file" }`.
    - `parseNamespacedToolName` handles tool names with underscores like `mcp__git_server__git_diff` into `{ serverId: "git_server", toolName: "git_diff" }`.
    - `parseNamespacedToolName` returns null for non-MCP tool names like `read_file` or invalid formats like `mcp__onlyone`.
    - `isMcpToolName` returns true for strings starting with `mcp__` and valid structure.
    - `extractToolCallsFromResponse` extracts tool calls from OpenAI response (`choices[0].message.tool_calls`).
    - `extractToolCallsFromResponse` extracts tool calls from Claude response (`content` blocks where `type === 'tool_use'`).
    - `extractToolCallsFromResponse` extracts tool calls from Gemini response (`candidates[0].content.parts` with `functionCall`).
    - `extractToolCallsFromResponse` extracts tool calls from Responses API response (`output` items where `type === 'function_call'`).
    - `partitionToolCalls` splits mixed tool calls into `mcpCalls` and `clientCalls`.
  </behavior>
  <action>Create RED Vitest tests in `tests/unit/mcp-tool-partition.test.js` testing all extraction formats and partitioning edge cases before implementing `open-sse/mcp/toolPartition.js`.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-tool-partition.test.js</automated>
  </verify>
  <done>Tests are written and fail due to missing modules or exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tool constants and toolPartition module</name>
  <files>open-sse/config/mcpConstants.js, open-sse/mcp/toolPartition.js</files>
  <behavior>
    - All Task 1 RED unit tests pass GREEN.
    - `open-sse/config/mcpConstants.js` exports `MAX_REACT_ITERATIONS` (10) and `MCP_TOOL_PREFIX` ("mcp__").
    - `open-sse/mcp/toolPartition.js` implements pure, immutable extraction and partitioning logic.
  </behavior>
  <action>Add constants to `open-sse/config/mcpConstants.js`. Implement `parseNamespacedToolName`, `isMcpToolName`, `extractToolCallsFromResponse`, and `partitionToolCalls` in `open-sse/mcp/toolPartition.js` handling JSON string arguments parsing safely with try/catch fallback.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-tool-partition.test.js</automated>
  </verify>
  <done>All tool partitioning and extraction unit tests pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_call output → tool parser | Model-generated tool names and arguments JSON may be malformed or adversarial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `parseToolArguments` | mitigate | Safely parse JSON strings with try/catch fallback, returning empty object or raw string if parsing fails. |
| T-04-02 | Elevation of Privilege | `parseNamespacedToolName` | mitigate | Require strict `mcp__{server}__{tool}` delimiter pattern and sanitize segments. |
| T-04-03 | Denial of Service | `extractToolCallsFromResponse` | mitigate | Handle null/undefined responses gracefully; never throw unhandled errors. |
</threat_model>

<verification>
Run `npm --prefix tests test -- tests/unit/mcp-tool-partition.test.js`.
</verification>

<success_criteria>
MCP-REACT-01 and MCP-REACT-02 requirements for detection, extraction, and partitioning of MCP tools vs client tools are tested and satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-autonomous-server-side-react-loop/04-01-SUMMARY.md` upon completion.
</output>
