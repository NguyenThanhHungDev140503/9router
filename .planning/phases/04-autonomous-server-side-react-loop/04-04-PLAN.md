---
phase: 04-autonomous-server-side-react-loop
plan: 04
type: tdd
wave: 3
depends_on:
  - 04-03
files_modified:
  - open-sse/handlers/chatCore.js
  - open-sse/handlers/chatCore/streamingHandler.js
  - src/sse/handlers/chat.js
  - tests/unit/mcp-chat-core-integration.test.js
autonomous: true
requirements:
  - MCP-REACT-01
  - MCP-REACT-02
  - MCP-REACT-03
  - MCP-REACT-04
  - MCP-REACT-05
user_setup: []
must_haves:
  truths:
    - "`chatCore.js` delegates request execution to `runToolLoop` when inbound MCP injection is active or tools are present."
    - "Intermediate ReAct turns run buffered (non-streaming) during a streaming request (Silent Buffering)."
    - "Only the final turn stream is piped to the client when the client requests `stream: true`."
    - "Non-streaming requests return the final synthesized JSON response with cumulative usage."
    - "Cumulative token usage across all turns updates the request detail log in `src/sse/handlers/chat.js` and usage database."
  artifacts:
    - path: "open-sse/handlers/chatCore.js"
      provides: "Main request execution path wired into ReAct tool loop."
      exports: ["handleChatCore"]
    - path: "open-sse/handlers/chatCore/streamingHandler.js"
      provides: "Final turn streaming response delivery."
      exports: ["handleStreamingResponse"]
    - path: "src/sse/handlers/chat.js"
      provides: "Ingress handler tracking cumulative token usage and request detail logging."
      exports: ["handleChatSSE"]
    - path: "tests/unit/mcp-chat-core-integration.test.js"
      provides: "Integration tests verifying end-to-end chatCore execution with ReAct loops in streaming and non-streaming modes."
  key_links:
    - from: "open-sse/handlers/chatCore.js"
      to: "open-sse/mcp/toolLoop.js"
      via: "invokes `runToolLoop` during request handling."
      pattern: "runToolLoop"
    - from: "open-sse/handlers/chatCore.js"
      to: "src/sse/handlers/chat.js"
      via: "returns aggregate response and cumulative usage metadata."
      pattern: "handleChatCore"
---

<objective>
Integrate `runToolLoop` into `open-sse/handlers/chatCore.js` and `src/sse/handlers/chat.js` with silent streaming buffering and cumulative usage reporting.

Purpose: Complete requirement MCP-REACT-05, tying the ReAct loop engine directly into the live gateway routing path.
Output: Integrated `chatCore.js`, `streamingHandler.js`, `src/sse/handlers/chat.js`, and comprehensive integration tests in `tests/unit/mcp-chat-core-integration.test.js`.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autonomous-server-side-react-loop/04-CONTEXT.md
@.planning/phases/04-autonomous-server-side-react-loop/04-RESEARCH.md
@open-sse/handlers/chatCore.js
@open-sse/handlers/chatCore/streamingHandler.js
@open-sse/handlers/chatCore/nonStreamingHandler.js
@src/sse/handlers/chat.js
@open-sse/mcp/toolLoop.js

<interfaces>
`handleChatCore({ body, headers, provider, model, modelConfig, account, isStreaming, signal, ... })`:
Returns:
- Streaming: Response with SSE body stream of final turn.
- Non-Streaming: Response with JSON body and cumulative usage.

Usage logging:
`saveRequestDetail(requestId, { ...detail, usage: cumulativeUsage })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing integration tests for ChatCore ReAct loop integration</name>
  <files>tests/unit/mcp-chat-core-integration.test.js</files>
  <behavior>
    - When no MCP tools are called, `handleChatCore` executes normally in a single turn.
    - When `mcp__*` tools are called in non-streaming mode, `handleChatCore` intercepts them, executes the loop, and returns the final JSON response with combined token usage.
    - When `mcp__*` tools are called in streaming mode (`stream: true`), intermediate turns do not emit SSE chunks to the client; only the final turn stream is piped to the client response stream.
    - Client-native tool calls are returned to the client in the final response without being intercepted or blocked.
    - Cumulative usage across all turns is reported in the response body (non-streaming) or metadata / request detail.
  </behavior>
  <action>Create RED Vitest integration tests in `tests/unit/mcp-chat-core-integration.test.js` mocking provider executors to verify end-to-end `handleChatCore` behavior with the ReAct tool loop.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-chat-core-integration.test.js</automated>
  </verify>
  <done>Tests are written and fail because `chatCore.js` is not yet wired to `runToolLoop`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire runToolLoop into chatCore.js and update usage tracking in src/sse/handlers/chat.js</name>
  <files>open-sse/handlers/chatCore.js, open-sse/handlers/chatCore/streamingHandler.js, src/sse/handlers/chat.js</files>
  <behavior>
    - All Task 1 RED integration tests pass GREEN.
    - `handleChatCore` seamlessly runs `runToolLoop` for intermediate execution turns.
    - Streaming silent buffering captures intermediate turns cleanly and pipes only the final turn.
    - Cumulative token usage is properly passed to `saveRequestDetail` and recorded in telemetry.
    - Regression tests pass with zero regressions against existing non-MCP requests.
  </behavior>
  <action>Wire `runToolLoop` into `open-sse/handlers/chatCore.js`. Support intermediate non-streaming turns during streaming requests. Update `src/sse/handlers/chat.js` to log cumulative usage.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-chat-core-integration.test.js tests/unit/mcp-tool-loop.test.js</automated>
  </verify>
  <done>ChatCore integration tests and full unit test suite pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Streaming Connection → Gateway | Client may disconnect mid-loop; gateway must abort upstream calls without leaking memory or hanging connections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Denial of Service | `streamingHandler.js` | mitigate | Bind client disconnect event (`req.signal` or `res.on('close')`) to `AbortSignal` passed to `runToolLoop`. |
| T-04-10 | Information Disclosure | `chatCore.js` | mitigate | Ensure intermediate MCP tool call payloads and system internals are not leaked in intermediate SSE chunks when streaming. |
</threat_model>

<verification>
Run `npm --prefix tests test -- tests/unit/mcp-chat-core-integration.test.js`.
Run regression baseline: `node tests/__baseline__/verify-no-regression.mjs`.
</verification>

<success_criteria>
MCP-REACT-01, MCP-REACT-02, MCP-REACT-03, MCP-REACT-04, and MCP-REACT-05 are completely satisfied, integrated, and verified.
</success_criteria>

<output>
Create `.planning/phases/04-autonomous-server-side-react-loop/04-04-SUMMARY.md` upon completion.
</output>
