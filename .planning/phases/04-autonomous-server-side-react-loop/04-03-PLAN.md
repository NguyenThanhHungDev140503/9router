---
phase: 04-autonomous-server-side-react-loop
plan: 03
type: tdd
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - open-sse/mcp/toolLoop.js
  - tests/unit/mcp-tool-loop.test.js
autonomous: true
requirements:
  - MCP-REACT-01
  - MCP-REACT-02
  - MCP-REACT-03
  - MCP-REACT-04
user_setup: []
must_haves:
  truths:
    - "`runToolLoop` coordinates multi-turn turn-by-turn communication with the LLM."
    - "Intermediate turns calling MCP tools execute the tools, inject results, and continue the loop."
    - "Mixed turns execute MCP tools first on Gateway, then allow the LLM to continue turn before yielding client-native tools."
    - "Terminal conditions are respected: no MCP tool calls, MAX_REACT_ITERATIONS (10) reached, or fatal error."
    - "Iteration capping or tool execution failure softly lands via an explanation turn with the LLM."
    - "Cumulative token usage across all turns is aggregated via `UsageAccumulator`."
  artifacts:
    - path: "open-sse/mcp/toolLoop.js"
      provides: "The autonomous ReAct loop engine executing turn-based tool loops."
      exports: ["runToolLoop"]
    - path: "tests/unit/mcp-tool-loop.test.js"
      provides: "Unit tests simulating multi-turn ReAct loops, mixed tools, max iterations cap, and error soft landing."
  key_links:
    - from: "open-sse/mcp/toolLoop.js"
      to: "open-sse/mcp/toolPartition.js"
      via: "uses `extractToolCallsFromResponse` and `partitionToolCalls`."
      pattern: "extractToolCallsFromResponse|partitionToolCalls"
    - from: "open-sse/mcp/toolLoop.js"
      to: "open-sse/mcp/toolExecutor.js"
      via: "uses `executeMcpToolCalls`."
      pattern: "executeMcpToolCalls"
    - from: "open-sse/mcp/toolLoop.js"
      to: "open-sse/mcp/contextInjector.js"
      via: "uses `injectTurnHistory`."
      pattern: "injectTurnHistory"
    - from: "open-sse/mcp/toolLoop.js"
      to: "open-sse/mcp/usageAccumulator.js"
      via: "uses `UsageAccumulator`."
      pattern: "UsageAccumulator"
---

<objective>
Implement the autonomous ReAct tool loop engine `open-sse/mcp/toolLoop.js` with comprehensive multi-turn simulation tests.

Purpose: Meet MCP-REACT-04 (and fulfill MCP-REACT-01..03 in orchestrator) by managing the iterative conversational cycle between upstream LLM and MCP tool execution.
Output: `open-sse/mcp/toolLoop.js` and `tests/unit/mcp-tool-loop.test.js`.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autonomous-server-side-react-loop/04-CONTEXT.md
@.planning/phases/04-autonomous-server-side-react-loop/04-RESEARCH.md
@open-sse/mcp/toolPartition.js
@open-sse/mcp/toolExecutor.js
@open-sse/mcp/contextInjector.js
@open-sse/mcp/usageAccumulator.js
@open-sse/config/mcpConstants.js

<interfaces>
`runToolLoop({ initialBody, sourceFormat, executorFn, isStreamingRequest, signal })`:
Parameters:
- `initialBody`: The initial request body (in `sourceFormat`).
- `sourceFormat`: Detected format (e.g. `FORMATS.OPENAI`, `FORMATS.CLAUDE`, etc.).
- `executorFn`: `async (currentBody, isIntermediate) => { rawResponse, parsedResponse, usage }`
- `isStreamingRequest`: boolean indicating whether client requested streaming.
- `signal`: AbortSignal for cancellation.

Returns:
`{ finalBody, finalResponse, turnsExecuted, cumulativeUsage, isClientToolCall }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for ReAct loop orchestration</name>
  <files>tests/unit/mcp-tool-loop.test.js</files>
  <behavior>
    - Loop exits on Turn 1 if no tool calls are returned by the LLM.
    - Loop exits on Turn 1 if only client-native tool calls are returned by the LLM.
    - Loop executes 2 turns when LLM calls an MCP tool on Turn 1 and provides final answer on Turn 2.
    - Loop executes 3+ turns when multiple MCP tools are called in sequence.
    - In mixed tool turns, MCP tools are executed first, results injected, and turn repeated.
    - Loop caps at `MAX_REACT_ITERATIONS` (10) and softly lands with a final explanation turn when LLM is in an infinite tool call loop.
    - Failed tool calls (e.g., server offline) are injected as error results into history, and LLM is given next turn to explain.
    - Cumulative usage correctly sums tokens across all turns.
  </behavior>
  <action>Create RED Vitest tests in `tests/unit/mcp-tool-loop.test.js` mocking `executorFn` and `executeMcpToolCalls` to verify all turn loop states and boundary conditions.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-tool-loop.test.js</automated>
  </verify>
  <done>Tests are written and fail because `open-sse/mcp/toolLoop.js` is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runToolLoop in open-sse/mcp/toolLoop.js</name>
  <files>open-sse/mcp/toolLoop.js</files>
  <behavior>
    - All Task 1 RED unit tests pass GREEN.
    - Iterates up to `MAX_REACT_ITERATIONS`.
    - Handles client abort signal gracefully.
    - Integrates tool partitioning, tool execution, context injection, and token accumulation seamlessly.
  </behavior>
  <action>Implement `runToolLoop` in `open-sse/mcp/toolLoop.js`. Coordinate `extractToolCallsFromResponse`, `partitionToolCalls`, `executeMcpToolCalls`, `injectTurnHistory`, and `UsageAccumulator`.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-tool-loop.test.js</automated>
  </verify>
  <done>All ReAct tool loop unit tests pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReAct Loop Execution Controller | Untrusted loop iterations could consume excessive compute or hang requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | `runToolLoop` | mitigate | Hard upper bound of `MAX_REACT_ITERATIONS` (10) prevents infinite looping; abort signal listener prevents lingering execution. |
| T-04-08 | Repudiation | `runToolLoop` | mitigate | Record turn counts and aggregate usage so complete telemetry is preserved. |
</threat_model>

<verification>
Run `npm --prefix tests test -- tests/unit/mcp-tool-loop.test.js`.
</verification>

<success_criteria>
MCP-REACT-04 requirement for multi-turn autonomous ReAct tool loop with iteration limits, soft landing error handling, and mixed call support is fully tested and satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-autonomous-server-side-react-loop/04-03-SUMMARY.md` upon completion.
</output>
