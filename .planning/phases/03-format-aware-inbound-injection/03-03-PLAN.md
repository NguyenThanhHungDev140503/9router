---
phase: 03-format-aware-inbound-injection
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - open-sse/mcp/inboundInjectionPipeline.js
  - open-sse/handlers/chatCore.js
  - tests/unit/mcp-chat-core-injection.test.js
autonomous: true
requirements:
  - MCP-INJECT-01
  - MCP-INJECT-02
  - MCP-INJECT-03
  - MCP-INJECT-04
user_setup: []
must_haves:
  truths:
    - "An incoming OpenAI, Claude, Gemini, Antigravity, or Responses request receives selected MCP tools and skills before upstream translation."
    - "If no enabled tools or skills exist, request body follows existing chat flow unchanged."
    - "An injection error logs only sanitized diagnostics and upstream request still uses original client body."
    - "Injection occurs after `sourceFormat` detection and before `translateRequest`, so correct source-format strategy is chosen."
    - "Reapplying injection to its prior output adds no duplicate `mcp__` tool or second gateway `<skills>` block while client tools and system text remain intact."
  artifacts:
    - path: "open-sse/mcp/inboundInjectionPipeline.js"
      provides: "Repository read, pure selection, format injector dispatch, idempotent skill injection, and fail-open re-entry boundary."
      exports: ["applyInboundInjection"]
    - path: "open-sse/handlers/chatCore.js"
      provides: "Inbound injection invocation immediately after source format is known and before translation/passthrough dispatch."
      contains: "applyInboundInjection"
    - path: "tests/unit/mcp-chat-core-injection.test.js"
      provides: "Mocked repository and executor integration coverage for order, no-op, header filtering, and exception fallback."
  key_links:
    - from: "open-sse/handlers/chatCore.js"
      to: "open-sse/mcp/inboundInjectionPipeline.js"
      via: "call after `sourceFormat` assignment and before `translateRequest`."
      pattern: "sourceFormat.*applyInboundInjection|applyInboundInjection.*translateRequest"
    - from: "open-sse/mcp/inboundInjectionPipeline.js"
      to: "src/lib/db/repos/mcpRepo.js"
      via: "`getEnabledMcpServers` and `getAllMcpToolsCache` read cached configuration only."
      pattern: "getEnabledMcpServers|getAllMcpToolsCache"
    - from: "open-sse/mcp/inboundInjectionPipeline.js"
      to: "src/lib/db/repos/skillsRepo.js"
      via: "`getEnabledSkills` provides prompt candidates."
      pattern: "getEnabledSkills"
    - from: "open-sse/mcp/inboundInjectionPipeline.js"
      to: "open-sse/mcp/injector.js"
      via: "selected canonical tools are converted through `createFormatInjector` before skill prompt injection."
      pattern: "createFormatInjector|injectSkillsPrompt"
---

<objective>
TDD inbound-injection pipeline and `chatCore` placement.

Purpose: connect Phase 3 format conversion, selection, and skill injection to real upstream request flow without adding server-side tool execution.
Output: fail-open pipeline, one pre-translation `chatCore` call site, and integration tests.
</objective>

<execution_context>
@/home/nguyen-thanh-hung/.codex/get-shit-done/workflows/execute-plan.md
@/home/nguyen-thanh-hung/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-format-aware-inbound-injection/03-CONTEXT.md
@.planning/phases/03-format-aware-inbound-injection/03-RESEARCH.md
@.planning/phases/02-mcp-process-manager-json-rpc-client/02-SUMMARY.md
@open-sse/AGENTS.md
@open-sse/handlers/chatCore.js
@open-sse/translator/formats.js
@open-sse/translator/formats/openai.js
@open-sse/translator/formats/claude.js
@open-sse/translator/formats/gemini.js
@open-sse/translator/formats/responsesApi.js
@src/lib/db/repos/mcpRepo.js
@src/lib/db/repos/skillsRepo.js
@src/lib/mcp/processManager.js
@src/lib/mcp/client.js

<interfaces>
From `open-sse/handlers/chatCore.js`:
`handleChatCore` assigns `const sourceFormat = sourceFormatOverride || detectFormat(body)` near line 73. Non-passthrough branch calls `translateRequest(sourceFormat, targetFormat, upstreamModel, body, ...)` near line 185. Place injection after source-format detection and bypass early return, before any translation or request mutation.

From `src/lib/db/repos/mcpRepo.js`:
Use only `getEnabledMcpServers()` and `getAllMcpToolsCache()` to read configuration/cache. No process start, tool execution, or cache write.

From `src/lib/db/repos/skillsRepo.js`:
Use only `getEnabledSkills()` to read active skill records.

From Phase 2:
`McpProcessManager.callServerTool()` and `McpClient.callTool()` execute MCP calls. Do not import or call either: Phase 4 owns namespaced-call execution.

From Plans 03-01 and 03-02:
Use `createFormatInjector`, `selectInboundMcp`, and `injectSkillsPrompt` as public contracts. Do not duplicate conversion, filtering, or XML rules in `chatCore`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing chatCore inbound-injection integration tests</name>
  <files>tests/unit/mcp-chat-core-injection.test.js</files>
  <behavior>
    - Mocked enabled server/cache/skill data causes source-format-native injected payload to reach mocked translator/executor.
    - Tests cover OpenAI plus one Claude/Gemini/Responses path and assert injection executes after source format detection but before `translateRequest`.
    - Empty enabled data preserves original body reference/shape and prior request result.
    - Header restriction, malformed cache, and a thrown injector error do not fail request; mocked translation receives original body on exception.
    - Passing a first `applyInboundInjection` result into a second call adds no duplicate `mcp__` tool or second gateway `<skills>` block, preserves client-owned tools/system text, and leaves original caller body unmutated.
  </behavior>
  <action>Create focused Vitest integration tests with module mocks for MCP/skill repositories and upstream executor/translator boundary. Include a two-call re-entry case: retain original caller body, call `applyInboundInjection` once, pass its returned body to second call, then assert one generated `mcp__` tool, one gateway-owned `<skills>` block, unchanged client tool/system text, and no mutation of original nested structures. Assert externally observable request body at translation or executor boundary rather than private local variables. Record RED failure before pipeline/call-site implementation. Keep tests hermetic; no real SQLite, MCP process, network, or Phase 4 tool call.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-chat-core-injection.test.js</automated>
  </verify>
  <done>Initial test run fails because `applyInboundInjection` and `chatCore` call site do not yet exist; tests define ordering, no-op, header, and fail-open contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build repository-backed fail-open pipeline and wire chatCore</name>
  <files>open-sse/mcp/inboundInjectionPipeline.js, open-sse/handlers/chatCore.js</files>
  <behavior>
    - All Task 1 integration cases pass.
    - Pipeline returns original body reference when repositories report no enabled tools/skills or selection finds no candidates.
    - Any repository, selection, conversion, or prompt-injection exception returns original body and keeps request flow alive.
    - Pipeline accepts prior `applyInboundInjection` output as input without duplicate namespaced tools or a second gateway skills block.
  </behavior>
  <action>Create `applyInboundInjection({ body, sourceFormat, headers, log })` as sole repository boundary and explicit gateway-owned retry contract. Read `getEnabledMcpServers`, `getAllMcpToolsCache`, and `getEnabledSkills`; call Plan 02 selection; use Plan 01 strategy to append selected MCP tools; then inject selected skill XML. On re-entry, preserve all client-owned tools and system text, rely on Plan 01 generated-name collision detection to skip already-injected `mcp__` tools, and rely on Plan 02 exact gateway marker detection to retain one skills block. Apply copy-on-write only after nonempty selection; never mutate caller body or nested client-owned arrays/objects. Catch all injection-stage exceptions, emit sanitized count/reason diagnostics through existing optional logger, and return exact original `body`. In `handleChatCore`, invoke this after `sourceFormat` detection and bypass decision but before provider thinking, modality changes, passthrough construction, and `translateRequest`; pass `clientRawRequest?.headers`. Do not alter client-native tools, translator implementation, DB repos, `processManager`, or `client` because Phase 4 owns execution.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-chat-core-injection.test.js</automated>
  </verify>
  <done>`chatCore` routes injected body through existing path only when selection is nonempty; errors and no-op inputs retain existing request behavior; retrying a prior pipeline result adds no MCP-tool or skills-block duplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove full Phase 3 regression boundary</name>
  <files>tests/unit/mcp-chat-core-injection.test.js, open-sse/mcp/inboundInjectionPipeline.js</files>
  <behavior>
    - One test proves all four native format strategies remain reachable through pipeline inputs.
    - One test proves client tool collision remains client-owned after full pipeline composition.
    - One test proves pipeline re-entry after full composition preserves client-owned tools/system text and retains exactly one generated MCP tool and gateway skills block.
    - One test proves fail-open log payload excludes raw schema, header, user prompt, and skill prompt text.
  </behavior>
  <action>Add final composition cases and refactor only shared pipeline control flow after GREEN. Run all three Phase 3 suites together, then current test suite. Do not broaden scope into ReAct looping, intercepting `mcp__*` responses, or tool invocation; namespaced calls remain outbound model metadata until Phase 4.</action>
  <verify>
    <automated>npm --prefix tests test -- tests/unit/mcp-format-injector.test.js tests/unit/mcp-inbound-selection.test.js tests/unit/mcp-skill-prompt-injector.test.js tests/unit/mcp-chat-core-injection.test.js && npm --prefix tests test</automated>
  </verify>
  <done>Integration confirms correct pre-translation ordering, all format strategies, no-op behavior, collision safety, sanitized fail-open diagnostics, and no regression in existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB repositories → pipeline | Cached external MCP schemas and enabled skill prompts enter request transformation. |
| Client headers/body → pipeline | Header filter and user text influence selected metadata. |
| Pipeline → `chatCore`/translator | Injection must not interrupt existing upstream routing, retry, or passthrough behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | `handleChatCore` injection call | mitigate | Single call site after `sourceFormat` and before `translateRequest`; reuse Plan 01 collision guard and Plan 02 immutable selection/prompt helpers. |
| T-03-13 | Tampering | `applyInboundInjection` retry/re-entry | mitigate | Copy-on-write input handling preserves caller-owned tools/system text; duplicate generated names are skipped and exact gateway-marked skills block is not appended twice. |
| T-03-10 | Elevation of Privilege | `x-mcp-servers` flow | mitigate | Pass header only into Plan 02 restrictive allow-list; never execute a tool or grant a server unavailable from enabled repository records. |
| T-03-11 | Denial of Service | pipeline failures | mitigate | Catch repository/selection/conversion/prompt errors at one boundary, return exact original body, and retain `MAX_INJECTED_TOOLS = 30`. |
| T-03-12 | Information Disclosure | fail-open diagnostics | mitigate | Logger receives fixed reason code and counts only; tests prohibit raw schemas, headers, user text, and skill prompts in log args. |
| T-03-SC | Tampering | package installs | accept | No package install in this plan; no supply-chain change. |
</threat_model>

<verification>
Run targeted integration suite, all Phase 3 unit suites, then `npm --prefix tests test`. Verify `chatCore` has one pre-translation pipeline invocation and no imports/calls to `McpProcessManager.callServerTool` or `McpClient.callTool`.
</verification>

<success_criteria>
All MCP-INJECT requirements are connected to `chatCore`. Injection is format-aware, selected from enabled cache/config only, copy-on-write and idempotent across retry/re-entry, no-op for no enabled/matching data, capped through config-owned `MAX_INJECTED_TOOLS = 30`, header-restrictable, and fail-open. Phase 4 execution remains untouched.
</success_criteria>

<output>
Create `.planning/phases/03-format-aware-inbound-injection/03-03-SUMMARY.md` when done.
</output>
